wew

@foreach ($product as $thisproduct)
        {{ $thisproduct }}
@endforeach

@foreach ($groupleader as $thisgroupleader)
        {{ $thisgroupleader }}
@endforeach

@foreach ($quantity as $thisquantity)
        {{ $thisquantity }}
@endforeach

@foreach ($moneypaid as $thismoneypaid)
        {{ $thismoneypaid }}
@endforeach

@foreach ($fullpayment as $thisfullpayment)
        {{ $thisfullpayment }}
@endforeach

@foreach ($change as $thischange)
        {{ $thischange }}
@endforeach